<form id="topicForm" method="post" action="/web/fotografia/<%= fotografia.id %>" >
  <input type="hidden" name="_method" value="put">
  <input type="hidden" name="id" value="<%= fotografia.id %>">

  <table class="table table-bordred table-striped">
    <thead>
      <tr>
        <th class="cell-stat text-center">id</th>
        <th class="cell-stat text-center">foto</th>
        <th class="cell-stat text-center">codArchivoFotografico</th>
        <th class="cell-stat text-center">numRollo</th>
        <th class="cell-stat text-center">numFoto</th>
        <th class="cell-stat text-center">fotografo</th>
        <th class="cell-stat text-center">fecha</th>
        <th class="cell-stat text-center">fechaCrea</th>
        <th class="cell-stat text-center">fechaModifica</th>
        <th class="cell-stat text-center">fechaBorra</th>
        <th class="cell-stat text-center">Accion</th>
        <th class="cell-stat text-center">Accion</th>
      </tr>
    </thead>
    <tbody>
        <tr>
          <td class="text-center">
            <%= fotografia.id %>
          </td>
          <td class="text-center">
            <label for="foto" class="control-label">Foto</label>
            <input type="text" class="form-control" id="foto" name="foto" value="<%=fotografia.foto%>" required="true" title="Ingrese foto" placeholder="<foto>">
            <%= fotografia.foto %>
          </td>
          <td class="text-center">
            <label for="codArchivoFotografico" class="control-label">Codigo de Archivo Fotografico</label>
            <input type="text" class="form-control" id="codArchivoFotografico" name="codArchivoFotografico" value="<%=fotografia.codArchivoFotografico%>" required="true" title="Ingrese Codigo de archivo fotografico" placeholder="<codArchivoFotografico>">
            <%= fotografia.codArchivoFotografico %>
          </td>
          <td class="text-center">
            <label for="numRollo" class="control-label">Numero Rollo</label>
            <input type="text" class="form-control" id="numRollo" name="numRollo" value="<%=fotografia.numRollo%>" required="true" title="Ingrese numero de rollo" placeholder="<numRollo>">
            <%= fotografia.numRollo %>
          </td>
          <td class="text-center">
            <label for="numFoto" class="control-label">Numero Foto</label>
            <input type="text" class="form-control" id="numFoto" name="numFoto" value="<%=fotografia.numFoto%>" required="true" title="Ingrese numero de foto" placeholder="<numFoto>">
            <%= fotografia.numFoto %>
          </td>
          <td class="text-center">
            <label for="fotografo" class="control-label">Fotografo</label>
            <input type="text" class="form-control" id="fotografo" name="fotografo" value="<%=fotografia.fotografo%>" required="true" title="Ingrese fotografo" placeholder="<fotografo>">
            <%= fotografia.fotografo %>
          </td>
          <td class="text-center">
            <label for="fecha" class="control-label">Fecha</label>
            <input type="text" class="form-control" id="fecha" name="fecha" value="<%=fotografia.fecha%>" required="true" title="Ingrese fecha" placeholder="<fecha>">
            <%= fotografia.fecha %>
          </td>
          <td class="text-center"><%= fotografia.fechaCrea %></td>
          <td class="text-center"><%= fotografia.fechaModifica %></td>
          <td class="text-center"><%= fotografia.fechaBorra %></td>
          <td>
            <button type="submit" name="commit" value="Form" class="btn btn-success btn-block">
              fechaModificar
            </button>
          </td>
          <td>
            <a href="/web/fotografia/<%= fotografia.id %>" class="btn btn-info" role="button">
              <p data-placement="top" data-toggle="tooltip" title="Edit">
                <button type="button" class="btn btn-info">Cancel
                </button>
              </p>
            </a>
          </td>
        </tr>
    </tbody>
  </table>
</form>
