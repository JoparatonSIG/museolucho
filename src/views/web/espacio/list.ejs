<table class="table table-bordred table-striped">
  <thead>
    <tr>
      <th class="cell-stat text-center">id</th>
      <th class="cell-stat text-center">espacio</th>
      <th class="cell-stat text-center">codigoEspacio</th>
      <th class="cell-stat text-center">inmuebles</th>
      <th class="cell-stat text-center">codigoInmueble</th>
      <th class="cell-stat text-center">ubicacionInmueble</th>
      <th class="cell-stat text-center">fechaCrea</th>
      <th class="cell-stat text-center">fechaModifica</th>
      <th class="cell-stat text-center">fechaBorra</th>
      <th class="cell-stat text-center">Accion</th>
      <th class="cell-stat text-center">Accion</th>
    </tr>
  </thead>
  <tbody>
    <% var i; for (i=0; i < espacioEJS.length; i++) { %>
      <tr>
        <td class="text-center"><%= espacioEJS[i].id %></td>
        <td class="text-center"><%= espacioEJS[i].espacio %></td>
        <td class="text-center"><%= espacioEJS[i].codigoEspacio %></td>
        <td class="text-center"><%= espacioEJS[i].inmuebles %></td>
        <td class="text-center"><%= espacioEJS[i].codigoInmueble %></td>
        <td class="text-center"><%= espacioEJS[i].ubicacionInmueble %></td>
        <td class="text-center"><%= espacioEJS[i].fechaCrea %></td>
        <td class="text-center"><%= espacioEJS[i].fechaModifica %></td>
        <td class="text-center"><%= espacioEJS[i].fechaBorra %></td>
        <td>
          <a href="/web/espacio/<%= espacioEJS[i].id %>" class="btn btn-info" role="button">
            <p data-placement="top" data-toggle="tooltip" title="Edit">
              <button type="button" class="btn btn-info">Edit</button>
            </p>
          </a>
        </td>
        <td>
          <form id="topicForm" method="post" action="/web/espacio/<%= espacioEJS[i].id %>" >
            <input type="hidden" name="_method" value="delete">
            <input type="hidden" name="id" value="<%= espacioEJS[i].id %>">
            <button type="submit" name="commit" value="Form" class="btn btn-success btn-block">
              Eliminar
            </button>
          </form>
        </td>
      </tr>
    <% } %>
  </tbody>
</table>
<form id="topicForm" method="get" action="/web/espacio/%>" >
    <% if (count > limitPage) { %>
    <% if (totalPage <= 8) { %>
      <% var j; for (j=1; j <= totalPage; j++) { %>
        <button type="submit" name="commit" value="Form" class="btn btn-success btn-block">
          <%=j%>
        </button>
      <% } %>
    <% } else { %>
      <% var j; for (j=1; j <= 3; j++) { %>
        <button type="submit" name="commit" value="Form" class="btn btn-success btn-block">
          <%=j%>
        </button>
      <% } %>
      <p>...</p>
      <% for (j=totalPage-3; j <= totalPage+1; j++) { %>
        <button type="submit" name="commit" value="Form" class="btn btn-success btn-block">
          <%=j%>
        </button>
      <% } %>
    <% } %>
    <% } %>
</form>
