<table class="table table-bordred table-striped">
  <thead>
    <tr>
      <th class="cell-stat text-center">id</th>
      <th class="cell-stat text-center">metodologia</th>
      <th class="cell-stat text-center">fechaRestauracion</th>
      <th class="cell-stat text-center">apellidoRestaurador</th>
      <th class="cell-stat text-center">descripcion</th>
      <th class="cell-stat text-center">fechaCrea</th>
      <th class="cell-stat text-center">fechaModifica</th>
      <th class="cell-stat text-center">fechaBorra</th>
      <th class="cell-stat text-center">Accion</th>
      <th class="cell-stat text-center">Accion</th>
    </tr>
  </thead>
  <tbody>
    <% var i; for (i=0; i < intervenciones.length; i++) { %>
      <tr>
        <td class="text-center"><%= intervenciones[i].id %></td>
        <td class="text-center"><%= intervenciones[i].metodologia %></td>
        <td class="text-center"><%= intervenciones[i].fechaRestauracion %></td>
        <td class="text-center"><%= intervenciones[i].apellidoRestaurador %></td>
        <td class="text-center"><%= intervenciones[i].descripcion %></td>
        <td class="text-center"><%= espacios[i].fechaCrea %></td>
        <td class="text-center"><%= espacios[i].fechaModifica %></td>
        <td class="text-center"><%= espacios[i].fechaBorra %></td>
        <td>
          <a href="/web/intervencion/<%= intervenciones[i].id %>" class="btn btn-info" role="button">
            <p data-placement="top" data-toggle="tooltip" title="Edit">
              <button type="button" class="btn btn-info">Edit</button>
            </p>
          </a>
        </td>
        <td>
          <form id="topicForm" method="post" action="/web/intervencion/<%= intervenciones[i].id %>" >
            <input type="hidden" name="_method" value="delete">
            <input type="hidden" name="id" value="<%= intervenciones[i].id %>">
            <button type="submit" name="commit" value="Form" class="btn btn-success btn-block">
              Eliminar
            </button>
          </form>
        </td>

    <!--    <td>
          <a href="/web/espacio/<%= espacios[i].id %>" class="btn btn-info" role="button">
            <p data-placement="top" data-toggle="tooltip" title="Edit">
              <button type="button" class="btn btn-info">Delete</button>
            </p>
          </a>
        </td>-->
      </tr>
    <% } %>
  </tbody>
</table>
<form id="topicForm" method="get" action="/web/intervencion/%>" >
    <% if (count > limitPage) { %>
    <% if (totalPage <= 8) { %>
      <% var j; for (j=1; j <= totalPage; j++) { %>
        <button type="submit" name="commit" value="Form" class="btn btn-success btn-block">
          <%=j%>
        </button>
      <% } %>
    <% } else { %>
      <% var j; for (j=1; j <= 3; j++) { %>
        <button type="submit" name="commit" value="Form" class="btn btn-success btn-block">
          <%=j%>
        </button>
      <% } %>
      <p>...</p>
      <% for (j=totalPage-3; j <= totalPage+1; j++) { %>
        <button type="submit" name="commit" value="Form" class="btn btn-success btn-block">
          <%=j%>
        </button>
      <% } %>
    <% } %>
    <% } %>
</form>
